#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back

void cUnorderSet(){
    unordered_set<int> st;
    // lower_bound and upper_bound function
    // does not works, rest all functions are same 
    // as above, it does not stores in any particular 
    // order it has a better complexity than set in most
    // cases, except some when collision happens 
}

int main() {
int t; cin >> t;
while(t--){
    cUnorderSet();
}

    return 0;
}